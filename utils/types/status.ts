
export enum BidStatus {
    PENDING = "PENDING",
    ACCEPTED = "ACCEPTED",
    FAILED = "FAILED"
}

export enum AuctionStatus {
    DRAFT = "DRAFT",
    PUBLISHED = "PUBLISHED",
    COMPLETED = "COMPLETED"
}

export enum DepositReason {
    BID = "BID",
    REFUND = "REFUND",
    DEPOSIT = "DEPOSIT"
}